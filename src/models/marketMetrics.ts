/**
 * MobulaLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { boolean, lazy, number, object, optional, Schema } from '../schema';
import { Approximations, approximationsSchema } from './approximations';

export interface MarketMetrics {
  marketCap?: number;
  liquidity?: number;
  price?: number;
  volume?: number;
  volume7d?: number;
  isListed?: boolean;
  approximations?: Approximations;
}

export const marketMetricsSchema: Schema<MarketMetrics> = object({
  marketCap: ['market_cap', optional(number())],
  liquidity: ['liquidity', optional(number())],
  price: ['price', optional(number())],
  volume: ['volume', optional(number())],
  volume7d: ['volume_7d', optional(number())],
  isListed: ['is_listed', optional(boolean())],
  approximations: [
    'approximations',
    optional(lazy(() => approximationsSchema)),
  ],
});
